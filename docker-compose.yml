version: '3'
services:
  server_HTTP:
    restart: always
    build: ./server_HTTP
    ports:
    - "8080:8080"
    networks:
    - server-tier

  client_mobile:
    restart: always
    build: ./client_mobile
    ports:
    - "8081:8081"
    networks:
    - server-tier

networks:
  server-tier:
    driver: bridge
